<% @trip.trails.each do |trail| %>
  Trail Name: <%= link_to trail.name, trail_path(trail) %>
  Length: <%= trail.length %>
  Address: <%= trail.address %>
<% end %>

Total Distance: <%= @trip.total_length %>
Average Distance: <%= @trip.average_length %>

Longest Trail on Trip Name:
<%= @trip.longest_trail.name if @trip.longest_trail %>
Length:
<%= @trip.longest_trail.length if @trip.longest_trail %>

Shortest Trail on Trip Name:
<%= @trip.shortest_trail.name if @trip.shortest_trail %>
Length:
<%= @trip.shortest_trail.length if @trip.shortest_trail %>
